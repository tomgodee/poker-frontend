import React from 'react';
import {
  FlexContainer,
} from './styles';

const RoomList = () => {
  return (
    <>
      <FlexContainer>
        This is room list component
      </FlexContainer>
    </>
  );
};

export default React.memo(RoomList);
